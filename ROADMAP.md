# Roadmap: AI News Curator Agent

## Цель

Автоматический агент на n8n, который ежедневно публикует 3-4 релевантных "горячих" новостных поста в Twitter по заданным темам.

---

## Архитектура пайплайна

```
[Источники] → [Сбор] → [Скоринг "горячести"] → [Фильтр топ-3-4] → [Рерайт через LLM] → [Публикация в Twitter]
```

**Запуск:** cron в n8n, 2-3 раза в день (утро, обед, вечер EST — прайм-тайм US аудитории)

---

## Блок 1: Сбор новостей

**Источники (ноды n8n):**
| Источник | Метод | Лимиты |
|----------|-------|--------|
| NewsAPI.org | HTTP Request | 100 запросов/день бесплатно |
| RSS-фиды | RSS Read нода | Без лимитов |
| Twitter/X API | HTTP Request | Basic tier — читать можно |
| Reddit API | HTTP Request | 60 запросов/мин |

**Статус:** ✅ Реализованы Python клиенты (`src/sources/`)

---

## Блок 2: Скоринг "горячести" — ключевой ML-блок

Гибридный скоринг — взвешенная формула с возможностью дообучения:

| Сигнал | Вес | Логика |
|--------|-----|--------|
| Свежесть | 0.25 | Экспоненциальный decay: `score = e^(-hours/12)`. Новость старше 24ч ≈ 0 |
| Виральность | 0.25 | Кол-во shares/retweets/upvotes нормализованное по источнику |
| Релевантность теме | 0.30 | Cosine similarity между embedding новости и embedding целевых тем |
| Сентимент/провокативность | 0.10 | Абсолютное значение sentiment score — нейтральные новости скучные |
| Уникальность | 0.10 | `1 - max_similarity` к уже опубликованным (антидубли) |

**Реализация в n8n:**
- Embeddings: OpenAI API (text-embedding-3-small) через HTTP Request ноду
- Sentiment: тот же LLM одним вызовом, prompt: `"Rate sentiment -1 to 1 and controversy 0 to 1. Return JSON only."`
- Остальные сигналы — математика в Code ноде (JavaScript)

**Статус:** ✅ Реализован Python модуль (`src/scoring/hot_score.py`)

---

## Блок 3: Рерайт через LLM

**Нода:** HTTP Request → OpenAI API (gpt-4o-mini)

**Промпт:**
```
You are a social media manager for a bicycle insurance company.
Rewrite this news as a Twitter post (max 260 chars).
Tone: professional but engaging, like an industry insider.
Add 1-2 relevant hashtags.
Do NOT copy original text — rephrase completely.
Do NOT use emoji excessively — max 1.
Include a hook in the first line.

Original: {title + summary}
Topic context: {configured topic}
```

**Важно:** генерировать 2 варианта, выбирать тот где длина ≤ 280 и есть hook в первой строке.

**Статус:** ✅ Реализован Python модуль (`src/rewrite/llm_rewriter.py`)

---

## Блок 4: Публикация

**Нода:** Twitter API v2

**Логика:**
- Проверить длину ≤ 280 символов
- Добавить ссылку на источник
- Если hot_score < 0.4 — не постить ничего (лучше молчать чем спамить)

**Статус:** ✅ Реализован Python модуль (`src/publish/twitter_post.py`)

---

## Блок 5: Хранилище и обратная связь

**Google Sheets или CSV (через n8n):**
- Лог всех собранных новостей + hot_score
- Лог опубликованных постов + engagement (likes, retweets — собирать через 24ч)
- Через 2-3 недели появится dataset для дообучения весов скоринга

**Фаза 2 (через месяц):** обучить простую модель (logistic regression) на данных: hot_score фичи → engagement. Заменить ручные веса на обученные.

**Статус:** ✅ Создана структура CSV (`data/news_log.csv`)

---

## Используемые сервисы

| Сервис | Зачем | Лимиты |
|--------|-------|--------|
| n8n Cloud | Оркестрация workflow | 14 дней триал, потом $20/мес |
| NewsAPI.org | Новости по ключевым словам | 100 запросов/день бесплатно |
| Google Sheet | Хранилище логов | Бесплатно |
| Twitter/X Developer | Читать + постить | Basic tier: 1500 твитов/мес |
| OpenAI API | Embeddings + рерайт | ~$5-15/мес |

---

## ✅ Что реализовано

### Структура проекта
```
hot-news-agent/
├── src/
│   ├── scoring/
│   │   ├── hot_score.py      ✅ Формула скоринга с весами
│   │   └── embeddings.py     ✅ Cosine similarity + OpenAI
│   ├── sources/
│   │   ├── newsapi.py        ✅ NewsAPI клиент
│   │   ├── rss.py            ✅ RSS парсер
│   │   └── twitter.py        ✅ Twitter API клиент
│   ├── rewrite/
│   │   └── llm_rewriter.py   ✅ Рерайт + sentiment анализ
│   └── publish/
│       └── twitter_post.py   ✅ Публикация + DryRun режим
├── config/
│   ├── topics.yaml           ✅ Темы (bicycle, e-bike, safety)
│   └── .env.example          ✅ Шаблон API ключей
├── data/
│   └── news_log.csv          ✅ Структура лога
└── tests/
    └── test_hot_score.py     ✅ Unit тесты скоринга
```

### Ключевые фичи в коде
- [x] Exponential decay для freshness: `e^(-hours/12)`
- [x] Нормализация виральности по источникам (twitter=1000, reddit=500, etc)
- [x] Cosine similarity для релевантности
- [x] Генерация 2 вариантов рерайта + выбор лучшего
- [x] DryRunPublisher для тестирования без Twitter
- [x] Примеры RSS фидов для велосипедной тематики

---

## 🚀 Следующие шаги

### Неделя 1: MVP
- [ ] Скопировать `.env.example` → `.env` и заполнить API ключи
- [ ] Зарегистрироваться в NewsAPI.org, получить ключ
- [ ] Настроить Twitter Developer аккаунт (Basic tier)
- [ ] Создать n8n workflow: RSS + NewsAPI → Google Sheet
- [ ] Добавить Code ноду со скорингом (без embeddings на старте)
- [ ] Добавить рерайт через HTTP Request → OpenAI
- [ ] Публикация в Twitter в dry-run режиме

### Неделя 2: Embeddings + Автоматизация
- [ ] Добавить embeddings для скоринга релевантности
- [ ] Настроить cron в n8n (3 раза в день)
- [ ] Включить реальную публикацию в Twitter
- [ ] Добавить сбор engagement через 24ч

### Неделя 3-4: Оптимизация
- [ ] Собрать данные engagement для дообучения весов
- [ ] A/B тест разных промптов для рерайта
- [ ] Добавить Reddit как источник
- [ ] Модерация через Slack/Telegram перед публикацией (опционально)

### Месяц 2: ML улучшения
- [ ] Обучить logistic regression на собранных данных
- [ ] Заменить ручные веса на обученные
- [ ] Добавить автоматическую ротацию тем

---

## Вопросы для уточнения

1. **Тематика** — велосипедное страхование? e-bike индустрия? cycling safety? шире?
2. **Twitter аккаунт** — уже есть? Какой tone of voice — экспертный, дружелюбный, провокативный?
3. **n8n** — self-hosted или n8n cloud?
4. **Бюджет на API** — OpenAI ~$5-15/мес, NewsAPI бесплатный тариф = 100 запросов/день
5. **Модерация** — нужна перед публикацией на первом этапе или сразу автомат?
