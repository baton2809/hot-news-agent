# Roadmap: AI News Curator Agent

## Цель

Автоматический агент на n8n, который ежедневно собирает 3-4 релевантных "горячих" новости, отправляет их для модерации в Slack и после одобрения публикует посты по заданным темам.

---

## Архитектура пайплайна

```
[RSS источники] → [Сбор] → [Скоринг "горячести" через ML] → [Фильтр топ-3-4] → [Рерайт через LLM] → [Slack модерация] → [Публикация после одобрения]
```

**Запуск:** cron в n8n, 2-3 раза в день (утро, обед, вечер EST — прайм-тайм US аудитории)

**Ключевые изменения (согласовано с заказчиком):**
- ✅ RSS как основной источник (Google News RSS: https://news.google.com/rss/search?q=ebike)
- ✅ Slack для предпоказа и модерации новостей перед публикацией
- ✅ Метрики собираются из RSS и определяются через ML модель (тренды, отсев шума)
- ✅ Совместное развертывание на n8n заказчика

---

## Блок 1: Сбор новостей

**Основной источник: RSS фиды**

**Приоритетные источники:**
| Источник | Метод | Лимиты | Пример |
|----------|-------|--------|--------|
| **Google News RSS** | RSS Read нода | Без лимитов | https://news.google.com/rss/search?q=ebike |
| **RSS-фиды конкретных сайтов** | RSS Read нода | Без лимитов | По велосипедной тематике |
| NewsAPI.org | HTTP Request | 100 запросов/день бесплатно | Резервный источник |
| Reddit API | HTTP Request | 60 запросов/мин | Опционально |

**Twitter API не используется** (платный, метрики по кейвордам будут минимальны)

**Статус:** ✅ Реализованы Python клиенты (`src/sources/`)
**TODO:** Добавить конкретные RSS фиды по велосипедной тематике

---

## Блок 2: Скоринг "горячести" — ключевой ML-блок

**Новый подход (согласовано с заказчиком):**
Метрики НЕ собираются с Twitter (по кейвордам их мало). Модель определяет тренды и отсеивает шум через анализ RSS данных.

Гибридный скоринг — взвешенная формула с возможностью дообучения:

| Сигнал | Вес | Логика | Источник данных |
|--------|-----|--------|-----------------|
| Свежесть | 0.25 | Экспоненциальный decay: `score = e^(-hours/12)`. Новость старше 24ч ≈ 0 | RSS pubDate |
| Частота упоминаний (тренд) | 0.25 | Количество статей с похожими темами за последние 6ч | RSS агрегация |
| Релевантность теме | 0.30 | Cosine similarity между embedding новости и embedding целевых тем | ML модель |
| Сентимент/провокативность | 0.10 | Абсолютное значение sentiment score — нейтральные новости скучные | ML модель |
| Уникальность | 0.10 | `1 - max_similarity` к уже опубликованным (антидубли) | ML модель |

**Реализация в n8n:**
- Embeddings: OpenAI API (text-embedding-3-small) через HTTP Request ноду
- Sentiment: тот же LLM одним вызовом, prompt: `"Rate sentiment -1 to 1 and controversy 0 to 1. Return JSON only."`
- Тренд-анализ: подсчет похожих статей через cosine similarity в Code ноде
- Остальные сигналы — математика в Code ноде (JavaScript)

**Статус:** ✅ Реализован Python модуль (`src/scoring/hot_score.py`)
**TODO:** Добавить тренд-анализ на основе частоты упоминаний в RSS

---

## Блок 3: Рерайт через LLM

**Нода:** HTTP Request → OpenAI API (gpt-4o-mini)

**Промпт:**
```
You are a social media manager for a bicycle insurance company.
Rewrite this news as a Twitter post (max 260 chars).
Tone: professional but engaging, like an industry insider.
Add 1-2 relevant hashtags.
Do NOT copy original text — rephrase completely.
Do NOT use emoji excessively — max 1.
Include a hook in the first line.

Original: {title + summary}
Topic context: {configured topic}
```

**Важно:** генерировать 2 варианта, выбирать тот где длина ≤ 280 и есть hook в первой строке.

**Статус:** ✅ Реализован Python модуль (`src/rewrite/llm_rewriter.py`)

---

## Блок 4: Модерация через Slack и публикация

**Slack модерация (приоритет):**
- **Нода:** Slack webhook/bot
- **Логика:**
  1. Отправка предпоказа новости в Slack канал
  2. Кнопки: "Одобрить" / "Отклонить"
  3. Публикация только после одобрения

**Публикация (после одобрения):**
- **Логика:**
  - Проверить длину ≤ 280 символов (если Twitter)
  - Добавить ссылку на источник
  - Если hot_score < 0.4 — не отправлять на модерацию (лучше молчать чем спамить)

**Статус:**
- ✅ Реализован Python модуль (`src/publish/twitter_post.py`)
- ⏳ TODO: Добавить Slack integration для модерации
- ⏳ TODO: Настроить workflow с одобрением через Slack

---

## Блок 5: Хранилище и обратная связь

**Google Sheets или CSV (через n8n):**
- Лог всех собранных новостей + hot_score
- Лог отправленных на модерацию новостей + решение (одобрено/отклонено)
- Статистика трендов и частоты упоминаний из RSS
- Через 2-3 недели появится dataset для дообучения весов скоринга на основе решений модератора

**Фаза 2 (через месяц):** обучить простую модель (logistic regression) на данных: hot_score фичи → решение модератора (одобрено/отклонено). Заменить ручные веса на обученные.

**Статус:** ✅ Создана структура CSV (`data/news_log.csv`)
**TODO:** Добавить логирование решений из Slack модерации

---

## Используемые сервисы

| Сервис | Зачем | Лимиты | Статус |
|--------|-------|--------|--------|
| n8n (self-hosted) | Оркестрация workflow | Бесплатно (заказчик разворачивает) | ✅ Согласовано |
| **Google News RSS** | Основной источник новостей | Без лимитов | ✅ Приоритет |
| RSS фиды сайтов | Новости по тематике | Без лимитов | ✅ Приоритет |
| NewsAPI.org | Резервный источник | 100 запросов/день бесплатно | Опционально |
| Google Sheet | Хранилище логов | Бесплатно | ✅ |
| **Slack** | Модерация и одобрение новостей | Бесплатно | ✅ Обязательно |
| OpenAI API | Embeddings + рерайт + тренд-анализ | ~$5-15/мес | ✅ |
| Twitter/X Developer | Публикация после одобрения | НЕ используется на первом этапе | ⏳ |

---

## ✅ Что реализовано

### Структура проекта
```
hot-news-agent/
├── src/
│   ├── scoring/
│   │   ├── hot_score.py      ✅ Формула скоринга с весами
│   │   └── embeddings.py     ✅ Cosine similarity + OpenAI
│   ├── sources/
│   │   ├── newsapi.py        ✅ NewsAPI клиент
│   │   ├── rss.py            ✅ RSS парсер
│   │   └── twitter.py        ✅ Twitter API клиент
│   ├── rewrite/
│   │   └── llm_rewriter.py   ✅ Рерайт + sentiment анализ
│   └── publish/
│       └── twitter_post.py   ✅ Публикация + DryRun режим
├── config/
│   ├── topics.yaml           ✅ Темы (bicycle, e-bike, safety)
│   └── .env.example          ✅ Шаблон API ключей
├── data/
│   └── news_log.csv          ✅ Структура лога
└── tests/
    └── test_hot_score.py     ✅ Unit тесты скоринга
```

### Ключевые фичи в коде
- [x] Exponential decay для freshness: `e^(-hours/12)`
- [x] Нормализация виральности по источникам (twitter=1000, reddit=500, etc)
- [x] Cosine similarity для релевантности
- [x] Генерация 2 вариантов рерайта + выбор лучшего
- [x] DryRunPublisher для тестирования без Twitter
- [x] Примеры RSS фидов для велосипедной тематики

### Новые задачи (по итогам встречи с заказчиком)
- [ ] Slack integration для модерации (отправка предпоказа, кнопки одобрения)
- [ ] Тренд-анализ: подсчет частоты упоминаний похожих новостей в RSS
- [ ] Google News RSS парсер (https://news.google.com/rss/search?q=ebike)
- [ ] Логирование решений модератора для обучения модели
- [ ] n8n workflow с полным циклом: RSS → Скоринг → Slack → Публикация
- [ ] Docker контейнер для совместного развертывания

---

## 🚀 Следующие шаги (обновлено по итогам встречи)

### Фаза 1: MVP с RSS + Slack модерацией
**Совместная работа с заказчиком**

- [ ] Подготовить код для развертывания (Python API + Docker)
- [ ] Заказчик разворачивает n8n на своем сервере
- [ ] Настроить Slack workspace и webhook для модерации
- [ ] Создать n8n workflow:
  1. RSS Read нода → Google News RSS (https://news.google.com/rss/search?q=ebike)
  2. RSS Read нода → дополнительные RSS фиды велосипедной тематики
  3. Code нода → скоринг "горячести" (без embeddings на старте)
  4. Filter нода → топ 3-4 новости (hot_score > 0.4)
  5. HTTP Request → OpenAI для рерайта
  6. Slack нода → отправка предпоказа с кнопками "Одобрить/Отклонить"
  7. Google Sheet → сохранение лога
- [ ] Демо: тестовый прогон workflow с реальными RSS данными

### Фаза 2: ML-скоринг и тренд-анализ
- [ ] Добавить embeddings для скоринга релевантности
- [ ] Реализовать тренд-анализ: подсчет частоты упоминаний похожих тем за последние 6ч
- [ ] Настроить cron в n8n (2-3 раза в день)
- [ ] Собирать статистику решений модератора (одобрено/отклонено) для дообучения

### Фаза 3: Автоматизация и публикация
- [ ] Интегрировать автоматическую публикацию после одобрения в Slack
- [ ] Добавить Reddit как дополнительный RSS источник
- [ ] A/B тест разных промптов для рерайта
- [ ] Настроить публикацию в выбранный канал (Twitter/LinkedIn/другое)

### Фаза 4: ML улучшения (через месяц)
- [ ] Обучить logistic regression на данных: hot_score фичи → решение модератора
- [ ] Заменить ручные веса на обученные
- [ ] Добавить автоматическую ротацию тем
- [ ] Оптимизация промптов на основе статистики

---

## ✅ Решенные вопросы (по итогам встречи)

1. **n8n** — ✅ self-hosted (заказчик разворачивает)
2. **Модерация** — ✅ через Slack перед публикацией (обязательно)
3. **Источники** — ✅ RSS (Google News + конкретные сайты), Twitter API не используется
4. **Метрики** — ✅ через ML модель (тренд-анализ, отсев шума), не с Twitter
5. **Развертывание** — ✅ совместное, код + демо + настройка вместе

## Открытые вопросы

1. **Тематика** — e-bike (подтверждено примером RSS), нужны дополнительные темы?
2. **Tone of voice** — для рерайта: экспертный, дружелюбный, провокативный?
3. **Канал публикации после Slack** — Twitter, LinkedIn, другое?
4. **Конкретные RSS фиды** — какие сайты велосипедной тематики добавить помимо Google News?
5. **Slack настройки** — уже есть workspace или создать новый?
