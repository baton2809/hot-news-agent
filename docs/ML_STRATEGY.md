# ML стратегия: Самообучение и оптимизация весов

## 🔍 Как работает СЕЙЧАС (MVP)

### Формула hot_score

```python
WEIGHTS = {
    'freshness': 0.25,    # Вручную установлено
    'virality': 0.25,     # Вручную установлено
    'relevance': 0.30,    # Вручную установлено
    'sentiment': 0.10,    # Вручную установлено
    'uniqueness': 0.10,   # Вручную установлено
}

hot_score = (
    freshness_score × 0.25 +
    virality_score  × 0.25 +
    relevance_score × 0.30 +
    sentiment_score × 0.10 +
    uniqueness_score × 0.10
)
```

**Проблема:** веса `0.25, 0.25, 0.30, 0.10, 0.10` — это **гипотеза**, не основаны на данных.

### Ручная классификация НЕ нужна! ❌

Вам **НЕ нужно** руками размечать новости "хорошая/плохая".

**Почему:** У нас есть **автоматический ground truth** — engagement в Twitter!

---

## 🤖 Автоматическое самообучение (Фаза 2)

### Идея: Engagement как целевая переменная

```
Опубликовали твит → через 24ч собрали метрики:
- Likes: 23
- Retweets: 7
- Replies: 3

→ engagement_score = 23 + 7×2 + 3×1 = 40

Это и есть "правда" - насколько новость была хорошая!
```

### Как работает обучение

**Шаг 1: Сбор данных (2-3 недели)**

Бот публикует, через 24ч собирает engagement:

| Title | Fresh | Viral | Relev | Sent | Uniq | **Likes** | **RT** | **Eng** |
|-------|-------|-------|-------|------|------|-----------|--------|---------|
| E-bike ban | 0.95 | 0.10 | 0.85 | 0.60 | 1.0 | 47 | 12 | 71 |
| Safety study | 0.70 | 0.05 | 0.90 | 0.30 | 0.95 | 23 | 5 | 33 |
| Random news | 0.50 | 0.02 | 0.40 | 0.20 | 0.80 | 3 | 0 | 3 |

**Шаг 2: Обучение модели**

```python
# Логистическая регрессия или gradient boosting
X = [freshness, virality, relevance, sentiment, uniqueness]
y = engagement_score

model.fit(X, y)

# Модель вернет НОВЫЕ веса:
new_weights = model.coef_
# Например: [0.15, 0.35, 0.40, 0.05, 0.05]
#            ^^^^  ^^^^  ^^^^  ^^^^  ^^^^
#          Виральность важнее свежести!
```

**Шаг 3: Обновление весов**

```python
WEIGHTS = {
    'freshness': 0.15,   # ↓ Было 0.25, стало 0.15
    'virality': 0.35,    # ↑ Было 0.25, стало 0.35
    'relevance': 0.40,   # ↑ Было 0.30, стало 0.40
    'sentiment': 0.05,   # ↓ Было 0.10, стало 0.05
    'uniqueness': 0.05,  # ↓ Было 0.10, стало 0.05
}
```

**Вывод:** Оказалось что виральность и релевантность важнее свежести!

---

## 📊 Три подхода к обучению

### Вариант 1: Linear Regression (простой)

**Что меняется:** только 5 весов

```python
from sklearn.linear_model import Ridge

model = Ridge(alpha=1.0)
model.fit(X_train, y_train)

# Новые веса
weights = model.coef_ / model.coef_.sum()  # Нормализация к сумме 1
```

**Плюсы:**
- ✅ Простой
- ✅ Интерпретируемый
- ✅ Нужно мало данных (50-100 твитов)

**Минусы:**
- ❌ Линейная зависимость (может быть неточно)

---

### Вариант 2: Gradient Boosting (средний)

**Что меняется:** веса + нелинейные взаимодействия

```python
from sklearn.ensemble import GradientBoostingRegressor

model = GradientBoostingRegressor(n_estimators=100)
model.fit(X_train, y_train)

# Feature importance показывает что важнее
importances = model.feature_importances_
# [0.15, 0.40, 0.30, 0.10, 0.05]
```

**Плюсы:**
- ✅ Точнее linear regression
- ✅ Находит нелинейности ("свежесть важна только если релевантность > 0.5")

**Минусы:**
- ❌ Нужно больше данных (200+ твитов)
- ❌ Сложнее интерпретировать

---

### Вариант 3: Online Learning (продвинутый)

**Что меняется:** веса обновляются каждый день

```python
from sklearn.linear_model import SGDRegressor

model = SGDRegressor()

# День 1
model.partial_fit(X_day1, y_day1)

# День 2
model.partial_fit(X_day2, y_day2)

# День 3...
# Веса постепенно корректируются
```

**Плюсы:**
- ✅ Адаптируется к трендам (что было hot месяц назад, сейчас не hot)
- ✅ Не нужно ждать 2-3 недели

**Минусы:**
- ❌ Может быть нестабильным
- ❌ Сложнее отлаживать

---

## 🎯 МОЯ РЕКОМЕНДАЦИЯ

### Фаза 1 (сейчас): Ручные веса
```python
WEIGHTS = {'freshness': 0.25, 'virality': 0.25, ...}
```
- Работает из коробки
- Достаточно для MVP

### Фаза 2 (через 2-3 недели): Linear Regression
```python
# Собрали 100-200 твитов с engagement
# Обучили модель раз в неделю
# Обновили веса

WEIGHTS = model.coef_  # Автоматически
```

### Фаза 3 (через 2 месяца): Gradient Boosting
```python
# 500+ твитов
# Находим нелинейности
# Точность +20-30%
```

---

## 📈 Пример: Что изменится

### До обучения (гипотеза):
```
Твит A: fresh=0.9, viral=0.1, relev=0.8
hot_score = 0.9×0.25 + 0.1×0.25 + 0.8×0.30 = 0.49

Твит B: fresh=0.6, viral=0.5, relev=0.9
hot_score = 0.6×0.25 + 0.5×0.25 + 0.9×0.30 = 0.55

→ Выбрали B
```

### После обучения (данные):
```
Оказалось что виральность важнее:
WEIGHTS = {'freshness': 0.15, 'virality': 0.40, 'relevance': 0.35}

Твит A: 0.9×0.15 + 0.1×0.40 + 0.8×0.35 = 0.455
Твит B: 0.6×0.15 + 0.5×0.40 + 0.9×0.35 = 0.605

→ Всё ещё B, но разница больше
```

**ИЛИ:**
```
Оказалось что свежесть переоценена:
WEIGHTS = {'freshness': 0.10, 'virality': 0.30, 'relevance': 0.50}

Твит A: 0.9×0.10 + 0.1×0.30 + 0.8×0.50 = 0.52
Твит B: 0.6×0.10 + 0.5×0.30 + 0.9×0.50 = 0.66

→ B победил с большим отрывом
```

---

## 🛠️ Как добавить самообучение (код)

### 1. Сбор engagement

Добавить в n8n workflow:

```javascript
// 24 часа после публикации
Schedule → HTTP Request (Twitter API):
  GET statuses/:tweet_id

Response:
  {
    "retweet_count": 12,
    "favorite_count": 47,
    "reply_count": 3
  }

// Сохранить в Google Sheets
```

### 2. Training script

```python
# src/ml/train_weights.py

import pandas as pd
from sklearn.linear_model import Ridge
from sklearn.model_selection import train_test_split

# Загрузить данные из Google Sheets
df = pd.read_csv('data/news_log.csv')

# Features
X = df[['freshness', 'virality', 'relevance', 'sentiment', 'uniqueness']]

# Target: engagement через 24ч
df['engagement'] = df['likes'] + df['retweets'] * 2 + df['replies']
y = df['engagement']

# Split
X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.2)

# Train
model = Ridge(alpha=1.0)
model.fit(X_train, y_train)

# Новые веса
new_weights = model.coef_ / model.coef_.sum()
print(f"Новые веса: {new_weights}")

# Сохранить
import json
with open('config/learned_weights.json', 'w') as f:
    json.dump({
        'freshness': float(new_weights[0]),
        'virality': float(new_weights[1]),
        'relevance': float(new_weights[2]),
        'sentiment': float(new_weights[3]),
        'uniqueness': float(new_weights[4])
    }, f)
```

### 3. Использовать обученные веса

```python
# src/scoring/hot_score.py

import json
import os

class HotScoreCalculator:
    def __init__(self, use_learned_weights=True):
        if use_learned_weights and os.path.exists('config/learned_weights.json'):
            with open('config/learned_weights.json') as f:
                self.WEIGHTS = json.load(f)
        else:
            # Дефолтные веса
            self.WEIGHTS = {
                'freshness': 0.25,
                'virality': 0.25,
                'relevance': 0.30,
                'sentiment': 0.10,
                'uniqueness': 0.10,
            }
```

---

## ❌ Что НЕ нужно делать

1. **Ручная разметка** ❌
   - НЕ нужно руками ставить "хорошая/плохая новость"
   - Engagement автоматический

2. **Кластеризация** ❌
   - НЕ нужна (это для unsupervised learning)
   - У нас supervised: есть target (engagement)

3. **Сложные ML модели** ❌
   - Не нужны нейронки на старте
   - Linear regression достаточно

---

## ⏱️ Timeline

| Неделя | Задача | Результат |
|--------|--------|-----------|
| 1-2 | Собрать 100 твитов | data/news_log.csv |
| 3 | Собрать engagement через 24ч | Добавлены likes/retweets |
| 4 | Обучить Ridge regression | learned_weights.json |
| 5 | Deploy новых весов | Точность +15-20% |
| 8 | Собрать 500 твитов | Обучить GBM |
| 12 | A/B тест | Сравнить старые vs новые веса |

---

## 🎯 Итого

**Отвечаю на вопросы:**

1. **Нужна ли ручная классификация?**
   - ❌ НЕТ! Engagement автоматический ground truth

2. **Будет ли самообучение?**
   - ✅ ДА! Через 2-3 недели можно обучить модель на engagement

3. **Какие веса поменяются?**
   - 5 коэффициентов: `freshness, virality, relevance, sentiment, uniqueness`
   - Сейчас: `0.25, 0.25, 0.30, 0.10, 0.10` (гипотеза)
   - После обучения: `?, ?, ?, ?, ?` (оптимизированные по данным)

4. **Как часто обновлять?**
   - Вариант 1: раз в месяц (manual)
   - Вариант 2: каждую неделю (auto)
   - Вариант 3: каждый день (online learning)

**Рекомендация:** начать с ручных весов, через 2-3 недели обучить Ridge regression раз в месяц.
