# Рабочие кейсы News Curator Agent

## 🎯 Обзор системы

Система автоматически собирает новости из RSS, анализирует их "горячесть" через ML, отправляет лучшие в Slack для модерации и публикует только после одобрения.

---

## 📋 Основные кейсы

### Кейс 1: Автоматический сбор и модерация (основной)

**Описание:** n8n запускается по расписанию (3 раза в день), собирает новости, отбирает топ-3-4 и отправляет в Slack на модерацию.

**Участники:**
- n8n workflow (автоматика)
- Python API (скоринг и рерайт)
- Модератор в Slack (человек)

**Шаги:**

1. **09:00 EST - n8n запускается по cron**
   ```
   Trigger: Cron (09:00, 13:00, 18:00)
   ```

2. **Сбор новостей из RSS**
   ```
   n8n RSS Read нода → Google News RSS
   URL: https://news.google.com/rss/search?q=ebike&hl=en-US
   Результат: 50-100 свежих новостей
   ```

3. **Скоринг через Python API**
   ```
   n8n → HTTP Request → http://python-api:8000/score

   Отправляет: массив новостей
   Получает: новости с hot_score (0-1)

   Сигналы скоринга:
   - Свежесть (25%) - экспоненциальный decay
   - Тренд (25%) - частота упоминаний за 6ч
   - Релевантность (30%) - cosine similarity к темам
   - Провокативность (10%) - sentiment анализ
   - Уникальность (10%) - отсев дублей
   ```

4. **Фильтрация**
   ```
   n8n Filter нода → hot_score >= 0.4
   Результат: только качественные новости
   ```

5. **Отбор топ-3-4**
   ```
   n8n Code нода → сортировка по hot_score + slice(0, 4)
   Результат: 3-4 лучшие новости
   ```

6. **Рерайт через Python API**
   ```
   n8n → HTTP Request → http://python-api:8000/rewrite

   Для каждой новости:
   - Генерация 2 вариантов твита через GPT-4o-mini
   - Выбор лучшего (длина ≤ 280, есть hook)
   - Sentiment/controversy анализ
   ```

7. **Отправка в Slack для модерации**
   ```
   n8n → HTTP Request → http://python-api:8000/slack/moderation

   Slack получает:
   - Красивую карточку для каждой новости
   - Оригинальный текст + рерайт
   - Hot score + дата публикации
   - Кнопки: ✅ Одобрить | ❌ Отклонить
   ```

8. **Модератор принимает решение**
   ```
   Модератор в Slack:
   - Читает предпоказ
   - Нажимает "Одобрить" или "Отклонить"
   ```

9. **Логирование в Google Sheets**
   ```
   n8n → Google Sheets нода

   Записывает:
   - timestamp
   - title
   - url
   - hot_score
   - rewritten_text
   - status: "pending_moderation"
   ```

**Результат:** Модератор получает в Slack 3-4 качественные новости для одобрения.

---

### Кейс 2: Публикация после одобрения (будущая фича)

**Описание:** После того как модератор нажимает "Одобрить" в Slack, новость автоматически публикуется.

**Статус:** 🚧 В разработке (требует Slack Interactivity)

**Шаги:**

1. **Модератор нажимает "Одобрить"**
   ```
   Slack Button Click → Slack API отправляет webhook
   ```

2. **n8n получает webhook от Slack**
   ```
   n8n Webhook нода → парсит action_id и news_id
   ```

3. **Обновление статуса в Google Sheets**
   ```
   Google Sheets: status = "approved"
   ```

4. **Публикация**
   ```
   n8n → Twitter API (или другая платформа)
   Пост: rewritten_text + url
   ```

5. **Уведомление в Slack**
   ```
   Slack: "✅ Опубликовано: [ссылка на твит]"
   ```

**Примечание:** Для этого кейса нужно настроить Slack App с Interactivity.

---

### Кейс 3: Ручной запуск workflow

**Описание:** Модератор хочет запустить сбор новостей вручную (не дожидаясь cron).

**Шаги:**

1. **Открыть n8n UI**
   ```
   http://localhost:5678
   Логин: admin / пароль из .env
   ```

2. **Открыть workflow "News Curator - Slack MVP"**

3. **Нажать "Execute Workflow"**
   ```
   n8n запускает весь пайплайн вручную
   ```

4. **Результат появляется в Slack через 30-60 секунд**

**Результат:** Мгновенный сбор новостей по требованию.

---

### Кейс 4: Тестирование без Slack

**Описание:** Разработчик хочет протестировать скоринг и рерайт без отправки в Slack.

**Шаги:**

1. **Отключить Slack ноду в n8n**
   ```
   Правый клик на "Send to Slack" → Disable
   ```

2. **Запустить workflow**

3. **Проверить результаты в n8n UI**
   ```
   Каждая нода показывает JSON output
   Можно увидеть:
   - Какие новости собраны
   - Их hot_score
   - Рерайты
   ```

**Результат:** Dry-run без реальной отправки в Slack.

---

### Кейс 5: Прямой вызов Python API (для отладки)

**Описание:** Разработчик хочет протестировать Python API напрямую.

**Шаги:**

1. **Health check**
   ```bash
   curl http://localhost:8000/health

   Ответ:
   {
     "status": "healthy",
     "openai_key": "set",
     "slack_webhook": "set",
     "scorer": "ready",
     "rewriter": "ready",
     "slack_moderator": "ready"
   }
   ```

2. **Тест скоринга**
   ```bash
   curl -X POST http://localhost:8000/score \
     -H "Content-Type: application/json" \
     -d '{
       "news": [{
         "title": "E-bike sales hit record high",
         "summary": "Electric bikes are taking over",
         "url": "https://example.com",
         "published_at": "2026-02-19T10:00:00Z",
         "source": "rss"
       }],
       "topic_keywords": ["ebike", "electric bicycle"],
       "top_n": 1
     }'

   Ответ:
   [{
     "title": "E-bike sales hit record high",
     "hot_score": 0.67,
     "freshness": 0.98,
     "trend_frequency": 0.45,
     "relevance": 0.82,
     ...
   }]
   ```

3. **Тест рерайта**
   ```bash
   curl -X POST http://localhost:8000/rewrite \
     -H "Content-Type: application/json" \
     -d '{
       "title": "E-bike sales hit record high",
       "summary": "Electric bikes are taking over the market",
       "url": "https://example.com",
       "topic": "e-bike industry"
     }'

   Ответ:
   {
     "tweet": "Electric bikes are reshaping urban mobility 🚴‍♂️ Sales hit record highs in Q1 2026 #EBike #Micromobility",
     "sentiment": 0.6,
     "controversy": 0.2,
     "length": 108
   }
   ```

4. **Тест Slack модерации**
   ```bash
   curl -X POST http://localhost:8000/slack/moderation \
     -H "Content-Type: application/json" \
     -d '{
       "news_items": [{
         "id": 1,
         "title": "Test news",
         "summary": "Testing Slack integration",
         "url": "https://example.com",
         "published": "2026-02-19",
         "hot_score": 0.7,
         "rewritten_text": "Test tweet"
       }]
     }'

   Результат: сообщение появляется в Slack
   ```

**Результат:** Прямая отладка API без n8n.

---

## 🔄 Полный цикл работы (End-to-End)

```
ВРЕМЯ         СИСТЕМА                  ДЕЙСТВИЕ                      УЧАСТНИК
─────────────────────────────────────────────────────────────────────────────

09:00 EST     n8n Cron                 Запуск workflow               Автомат
09:00:05      n8n RSS                  Сбор 100 новостей             Автомат
09:00:10      Python API /score        Скоринг + тренд-анализ        Автомат
09:00:15      n8n Filter               Отбор score >= 0.4            Автомат
09:00:16      n8n Code                 Топ-3-4 новости               Автомат
09:00:17      Python API /rewrite      Рерайт x4 новостей            Автомат
09:00:25      Python API /slack        Отправка в Slack              Автомат
09:00:26      Slack                    Карточки с кнопками           Автомат
09:00:30      n8n Google Sheets        Логирование                   Автомат

[Модератор читает в Slack, принимает решение в течение дня]

10:30         Модератор                Нажимает "Одобрить" (x3)      Человек
              [Будущая фича]           → Публикация в Twitter        Автомат

13:00 EST     n8n Cron                 Следующий цикл                Автомат
...
```

---

## 📊 Метрики и аналитика

### Google Sheets лог содержит:

| timestamp | title | url | hot_score | rewritten_text | status |
|-----------|-------|-----|-----------|----------------|--------|
| 2026-02-19 09:00:30 | E-bike sales surge | https://... | 0.72 | "Electric bikes..." | pending_moderation |
| 2026-02-19 09:00:31 | New bike lanes | https://... | 0.65 | "Cities expand..." | pending_moderation |

**Анализ после 2-3 недель:**

1. **Какие темы получают больше одобрений**
   - Фильтр по `status = "approved"`
   - Группировка по ключевым словам в title

2. **Какой hot_score коррелирует с одобрением**
   - Обучение модели: `hot_score_features → approved/rejected`
   - Замена ручных весов (0.25/0.25/0.30/0.10/0.10) на обученные

3. **Оптимальное время публикации**
   - Когда модераторы чаще одобряют
   - Корректировка cron расписания

---

## 🛠️ Технические детали

### Python API Endpoints:

| Endpoint | Метод | Описание |
|----------|-------|----------|
| `/health` | GET | Health check |
| `/score` | POST | Скоринг новостей (топ-N) |
| `/rewrite` | POST | Рерайт одной новости |
| `/score-and-rewrite` | POST | Комбо (скоринг + рерайт) |
| `/slack/moderation` | POST | Отправка в Slack с кнопками |

### n8n Workflow ноды:

1. **Manual Trigger** или **Cron** - запуск
2. **RSS Read** - сбор Google News
3. **HTTP Request** - Python API /score
4. **IF** - фильтр score >= 0.4
5. **Code** - топ-3-4
6. **HTTP Request** - Python API /rewrite (для каждой)
7. **HTTP Request** - Python API /slack/moderation
8. **Google Sheets** - лог

### Docker контейнеры:

- `news-curator-api` (Python FastAPI) - порт 8000
- `news-curator-n8n` (n8n) - порт 5678
- Общая сеть `news-curator`

---

## 🚀 Быстрый старт для нового пользователя

1. **Клонировать репозиторий**
2. **Настроить `.env`** (OPENAI_API_KEY, SLACK_WEBHOOK_URL)
3. **`docker-compose up -d`**
4. **Открыть http://localhost:5678**
5. **Импортировать workflow** (автоматически загружен)
6. **Активировать workflow**
7. **Нажать "Execute Workflow"** (ручной запуск)
8. **Проверить Slack** - должны появиться новости

**Время до первых результатов: 5 минут**

---

## 📝 Частые вопросы

### Можно ли использовать без Slack?

Да, можно отключить Slack ноду и просто логировать в Google Sheets. Модерация будет через Sheets.

### Можно ли публиковать не в Twitter?

Да, можно добавить любую платформу: LinkedIn, Telegram, Discord, Email и т.д.

### Сколько стоит запустить?

- VPS: $5-10/мес (2GB RAM)
- OpenAI API: $5-15/мес
- Slack: бесплатно
- **Итого: $10-25/мес**

### Нужно ли знать программирование?

Для базового использования - нет. Все настраивается через n8n UI.
Для кастомизации - желательно знание JavaScript (для Code нод).
